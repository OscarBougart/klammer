/**
 * The board — E2. Placeholder until the field zones land.
 */
import { Text, View } from 'react-native';

export default function SessionScreen() {
  return (
    <View className="flex-1 bg-schiefer items-center justify-center">
      <Text className="font-display-semibold text-tile text-kreide">Das Brett</Text>
    </View>
  );
}
