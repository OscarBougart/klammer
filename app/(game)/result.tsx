/**
 * End of session — E4.3. Placeholder until the ten-sentence board lands.
 */
import { Text, View } from 'react-native';

export default function ResultScreen() {
  return (
    <View className="flex-1 bg-schiefer items-center justify-center">
      <Text className="font-display-semibold text-tile text-kreide">Fertig</Text>
    </View>
  );
}
